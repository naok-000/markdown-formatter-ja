pre-commit:
  parallel: true
  jobs:
    - name: quality-gate
      glob: "*.rs"
      run: scripts/quality-gate.sh

pre-push:
  parallel: true
  jobs:
    - name: quality-gate
      run: scripts/quality-gate.sh

    - name: codex-review
      run: scripts/codex-review.sh --base main
